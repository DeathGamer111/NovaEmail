# Synthetic mail test lab

This directory is reserved for local, synthetic IMAP/SMTP interoperability fixtures. Runtime credentials, certificates, server state, and environment files are ignored and must not be committed.
